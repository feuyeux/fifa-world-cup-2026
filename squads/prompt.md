根据输入的“小组球队资料文件 + 对应球队图片文件”，生成 26 人现代体育商业海报。
目标是最大化多次生成一致性。以下固定项均为硬性约束，禁止自行发挥。

---

### 输入规范（先解析）

你会收到两类输入文件（示例）：
- 球队名单与结构化信息：squads/L_Croatia.md
- 球队视觉参考图：squads/L_Croatia.png

解析规则（强制）：
- 两个文件必须同 basename（如 L_Croatia）。
- 组别来自文件名前缀字母（如 L_ -> Group L）。
- 球队名来自 basename 下划线后的部分（如 Croatia）。
- 若用户提供的球队名与文件名冲突，以文件名为准。

数据抽取规则（强制）：
- 仅使用该 .md 文件中的球员表。
- 只识别表头字段：号码、位置、姓名、出生日期、俱乐部。
- 姓名展示格式固定：中文名 / English Name。
- 位置仅允许 GK/DF/MF/FW。

人数约束（默认 26 人制）：
- 总人数必须为 26。
- 位置人数必须为：GK=3, DF=8, MF=10, FW=5。
- 若输入文件不满足上述分布，先报错并停止，不得强行生成。

---

### 基本信息（由输入自动填充）

- 主题球队：由文件名自动识别（如 Croatia）
- 赛事：2026 年世界杯
- 名单：严格使用输入 .md 的全部且仅有球员，不得增减
- 球衣参考：必须使用同 basename 的 .png（如 squads/L_Croatia.png）
- 分组校验：可用 jerseys/_groups.md 进行组别与球队名交叉检查；若不一致，报告冲突并停止

---

### 一致性参数锁定（硬性）

- 输出尺寸固定：2400 x 1350 px，16:9
- 视角固定：正面平视，镜头高度胸口位
- 焦段固定：等效 50mm，不使用广角畸变
- 光照固定：主光左上 45 度 + 右侧弱补光 + 顶部轮廓光
- 调色固定：高对比电影体育风，整体冷暖平衡不漂移
- 噪点与锐化固定：中低噪点，统一锐度
- 随机性固定：若引擎支持 seed，则固定 seed=20260603；若不支持 seed，则严格复用同一构图与参数模板
- 出图内容固定：仅展示 26 人头像（头部至肩部），禁止半身、全身或任何单人特写镜头

---

### 固定画布网格与槽位（核心）

使用固定 4 列分区，从左到右：GK / DF / MF / FW。
列内使用固定槽位，不允许自由挪动。

- 画布安全边距：左 90，右 90，上 90，下 90
- 四个分组列中心 x 坐标固定：GK=300，DF=870，MF=1530，FW=2100
- 每个分组采用 2 列子网格，槽位按“从上到下、从左到右”填充
- 每名球员统一人物框：170 x 210（宽 x 高）
- 同一网格行使用同一行基线（row baseline），同一槽位内人物脚底贴合本槽位基线
- 槽位纵向步进固定为 220，槽位横向步进固定为 185
- 每名球员头像安全区固定：人物主体需落在槽位中心 160 x 190 区域内，四周至少保留 10 px 留白
- 槽位空间保障：任意两名球员头像外接框之间最小间距 >= 12 px，禁止被特效元素侵占

人数映射规则（强制）：
- GK 列 3 人
- DF 列 8 人
- MF 列 10 人
- FW 列 5 人

排列规则（强制）：
- 同一分组内先按号码升序，再按子网格填充顺序落位
- 同列同尺寸、同对齐、同间距
- 禁止重叠、禁止遮挡姓名、禁止跨列

---

### 人物尺度与姿态统一

- 26 人必须同一身体尺度，不因球星身份改变缩放
- 头顶在槽位内的相对高度允许误差 <= 2%
- 肩宽视觉比例允许误差 <= 3%
- 姿态统一为头像海报姿态（正面或微侧，头部至肩部），允许轻微表情差异，但不得改变人物框占位
- 若需突出核心球员，只允许通过姓名文字样式轻度高亮（如字重或描边），禁止放大人物、禁止新增单人特写框

---

### 文本系统固定

- 四列标题固定：GK / DF / MF / FW
- 标题字号、字重、颜色四列完全一致
- 球员标注格式固定：中文名 / English Name  #号码
- 所有姓名文本框宽度一致，超长自动缩小到同一最小字号，不换行溢出
- 文本颜色与描边固定，保证在任何背景上可读

---

### 背景与视觉层级固定

- 背景仅使用该队主色 + 辅助色渐变（可从球队常用色与参考图提取），不更换主题方向
- 国旗元素仅做低透明叠加，不可喧宾夺主
- 球场灯光、草坪纹理、战术线条透明度固定在弱层级
- 主体层级优先级：球员 > 姓名号码 > 分组标题 > 背景特效
- 背景特效不得进入球员头像安全区，不得压缩任意球员可见面积

---

### 负面约束（严格禁止）

禁止输出非 2400x1350。
禁止输出非 16:9。
禁止出现第 27 人或任何额外人像元素。
禁止任何单人特写、裁切放大或镜头推近效果。
禁止使用与输入 .png 不一致的球衣颜色、花纹、领口、袖口、徽标或号码风格。
禁止混用其他球队图片的球衣元素。
禁止任何球员尺寸偏大或偏小。
禁止同一网格行脚底不齐、禁止槽位内头顶高度明显不齐。
禁止头像被边缘裁切、禁止头像超出槽位安全区。
禁止特效、标题、旗帜、光斑遮挡头像关键区域（眼睛/鼻子/嘴/肩线）。
禁止分组错位、跨列、错人数。
禁止缺人、重复人、虚构人。
禁止姓名不可读、号码错误、队徽错误、球衣颜色错误。
禁止明显重复脸、解剖错误、手指畸形。

---

### 生成流程（先校验后出图）

第一步：输入校验
- 校验文件是否成对存在：squads/<basename>.md 与 squads/<basename>.png。
- 校验 .md 表格字段是否完整、总人数是否为 26、位置分布是否为 3/8/10/5。

第二步：结构校验
- 检查列映射、槽位数量、排序规则是否完全匹配。

第三步：版式校验
- 检查 4 列坐标、统一人物框、统一基线、统一字号是否全部生效。
- 检查 26 个头像是否全部位于各自槽位安全区，且最小间距 >= 12 px。
- 检查是否存在任何单人特写层或额外人像层，若存在则直接判定失败。

第四步：球衣一致性校验
- 逐一检查 26 人球衣是否与同 basename 的 .png 参考图一致。
- 若任一球员球衣与参考图不一致，则整张图判定失败并重生。

第五步：最终渲染
- 在通过前四步后再渲染高清成图，不允许边渲染边改布局。

---

### 最终验收清单

- 尺寸是否为 2400x1350
- 是否恰好 26 人
- 是否 GK3/DF8/MF10/FW5
- 是否全部为头像构图（头部至肩部），且无单人特写
- 是否每人都有完整可见区域、无遮挡、无越界
- 是否所有球员球衣均与输入 .png 一致
- 是否所有人物同尺度且符合槽位基线规则
- 是否标题与姓名体系统一可读
- 是否无错人、无重复、无错误号码
- 是否达到可直接发布的商业海报质感
